extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main
  .container.my-3 
    .row
      .col-12 
        +box-head(pageTitle)
    
    .row 
      each topic of topics
        .col-4.mb-3 
          div(class="card")
            img.card-img-top(src=topic.avatar, alt="Card image cap")
            .card-body
              h5.card-title 
                a(href="/topics/1") #{topic.title}
              p.card-text #{topic.description}
              a.btn.btn-primary(href=`songs/${topic.slug}`) Xem chi tiết
      