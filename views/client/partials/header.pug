

header.header
  .container
    .row.align-items-center
      .col-3
        .inner-logo
          a(href="/")
            img(src="/image/logo.svg", alt="Logo")

      .col-5
        form(
          action="/search/result"
          method="GET"
        ) 
          div(class="form-group d-flex mb-0")
            input(
              type="text"
              placeholder="Nhập từ khóa..."
              class="form-control"
              name="keyword"
              value=keyword
            )
            button(
              type="submit"
              class="btn btn-primary"
            ) Tìm

      .col-4
        .inner-menu
          ul
            li
              a(href="/") Trang chủ
            li
              a(href="/products") Chủ đề
            li
              a(href="/favorite-songs") Bài hát yêu thích
              